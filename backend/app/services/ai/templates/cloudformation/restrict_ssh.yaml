Resources:
  MySecurityGroupIngress:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: "{{ resource_id }}"
      IpProtocol: tcp
      FromPort: 22
      ToPort: 22
      CidrIp: "<vpn-cidr>"
