package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// --- Bento Grid Palette Theme ---
val BentoBg = Color(0xFFFBF8FF)             // Light, warm-tinted canvas background
val BentoTextDark = Color(0xFF1D1B20)       // Deep, high-contrast graphite body text
val BentoTextSubtitle = Color(0xFF49454F)   // Muted gray-purple for small captions
val BentoPurplePrimary = Color(0xFF6750A4)  // Royal Material 3 purple for interactive buttons
val BentoPurpleDark = Color(0xFF21005D)     // Deep indigo-purple for headlines & focus
val BentoContainerActive = Color(0xFFEADDFF)// Lavish active pill containers
val BentoContainerMuted = Color(0xFFE8DEF8) // Soft lavender-grey secondary surfaces
val BentoBorderLight = Color(0xFFD0BCFF)    // Delicate violet outline indicators
val BentoBorderMedium = Color(0xFFCAC4D0)   // Classical M3 medium dividers/borders
val BentoPromoBg = Color(0xFFF3EFFF)        // Highlights or action focus background
val BentoAccentRed = Color(0xFFB3261E)      // Vibrant warning and active threat notification red
val BentoTermGreen = Color(0xFF1B5E20)      // Healthy status and active execution logs green

// --- Multi-Cloud Brand Accents ---
val AWSColor = Color(0xFFFF9900)            // High-visibility AWS Orange
val AzureColor = Color(0xFF0089D6)          // Structure Azure Azure Blue
val GCPColor = Color(0xFF4285F4)            // Analysis GCP Blue
val AWSLightBg = Color(0xFFFFF2E0)
val AzureLightBg = Color(0xFFE0F2FE)
val GCPLightBg = Color(0xFFE8F0FE)

// --- Additional Cosmic / Dark Theme Colors ---
val SpaceSlate = Color(0xFF0F0E13)   // Very dark background
val DeepCard = Color(0xFF151419)     // Slightly lighter card/surface
val BorderGrey = Color(0xFF2C2A35)   // Muted grey border
val CyberCyan = Color(0xFF00E5FF)    // Vibrant cyan accent
val TextWhite = Color(0xFFFFFFFF)    // Plain white text
val TerminalGreen = Color(0xFF00FF66) // Neon green terminal logs
val WarningAmber = Color(0xFFFF9900)  // Amber warning and alerts
val ElectricBlue = Color(0xFF2979FF)  // Intense electrical blue
val NebulaPurple = Color(0xFF7C4DFF)  // Deep mystic violet/purple
val TextDim = Color(0xFF9E9BA8)      // Dim secondary gray-white text


